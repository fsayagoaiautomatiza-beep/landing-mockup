---
// Video ID extracted from the URL
const videoId = "99cc08f7-48c1-47eb-b61d-b56fd5e98e57";
---

<section class="relative pt-32 sm:pt-40 pb-16 sm:pb-24 px-4 sm:px-6 min-h-screen flex items-center overflow-hidden">
    <!-- Subtle background accent - very light -->
    <div class="absolute top-0 left-0 right-0 h-[70vh] bg-gradient-to-b from-blue-50/50 via-transparent to-transparent pointer-events-none"></div>
    
    <div class="max-w-7xl mx-auto w-full relative z-10">
        <!-- Centered content layout for maximum impact - Above the Fold -->
        <div class="flex flex-col items-center text-center max-w-4xl mx-auto">
            
            <!-- Badge - Social proof (Above the fold question: "What do you do?") -->
            <div 
                class="inline-flex items-center gap-2.5 px-4 sm:px-5 py-2.5 rounded-full bg-slate-50 border border-slate-200/80 text-slate-600 text-xs sm:text-sm font-medium mb-8 sm:mb-10 shadow-sm"
                data-animate="fade-up"
            >
                <span class="flex items-center justify-center w-5 h-5 rounded-full bg-blue-100">
                    <svg class="w-3 h-3 text-blue-600" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
                    </svg>
                </span>
                <span><span class="text-blue-600 font-semibold">+60</span> clínicas ya confían en nosotros</span>
            </div>
            
            <!-- Main Headline - Clear value proposition -->
            <h1 
                class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold leading-[1.05] mb-6 sm:mb-8 tracking-tight text-slate-900"
                data-animate="fade-up"
                data-animate-delay="100"
            >
                Convierte cada contacto<br />
                en un <span class="text-gradient">nuevo paciente.</span>
            </h1>
            
            <!-- Subheadline - Answer: "How does it help me?" -->
            <p 
                class="text-base sm:text-lg md:text-xl text-slate-500 mb-10 sm:mb-12 leading-relaxed max-w-2xl px-4"
                style="line-height: 1.65;"
                data-animate="fade-up"
                data-animate-delay="200"
            >
                Aumenta tus ventas y automatiza la conversión de tus leads con un sistema inteligente que <span class="text-slate-700 font-medium">contacta, agenda y da seguimiento</span> por ti las 24h del día.
            </p>
            
            <!-- CTA Button - Answer: "What do I need to do now?" -->
            <div 
                class="relative mb-16 sm:mb-20"
                data-animate="fade-up"
                data-animate-delay="300"
            >
                <a 
                    href="/demo" 
                    class="btn-primary relative text-base sm:text-lg px-10 sm:px-12 py-4 sm:py-5 group inline-flex items-center gap-3"
                    id="hero-cta"
                >
                    Solicitar Demo Gratis
                    <svg class="w-5 h-5 transition-transform group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
                    </svg>
                </a>
                
                <!-- Trust indicators below CTA -->
                <div class="flex flex-wrap items-center justify-center gap-4 sm:gap-6 mt-6 text-xs sm:text-sm text-slate-400">
                    <div class="flex items-center gap-1.5">
                        <svg class="w-4 h-4 text-green-500" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                        </svg>
                        <span>Sin compromiso</span>
                    </div>
                    <div class="flex items-center gap-1.5">
                        <svg class="w-4 h-4 text-green-500" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                        </svg>
                        <span>Setup en 48h</span>
                    </div>
                    <div class="flex items-center gap-1.5">
                        <svg class="w-4 h-4 text-green-500" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                        </svg>
                        <span>30 min demo</span>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Video Section - Full width, premium presentation -->
        <div 
            class="mt-8 sm:mt-12 max-w-5xl mx-auto"
            data-animate="scale"
            data-animate-delay="400"
        >
            <div class="relative">
                <!-- Soft shadow effect - "floating" card -->
                <div class="absolute -inset-3 sm:-inset-4 bg-gradient-to-b from-blue-100/40 via-violet-100/30 to-transparent rounded-3xl blur-2xl opacity-70" aria-hidden="true"></div>
                
                <!-- Video container with premium styling -->
                <div class="relative bg-white rounded-2xl sm:rounded-3xl p-1.5 sm:p-2 shadow-2xl border border-slate-200/60 overflow-hidden group">
                    <!-- Subtle gradient border on hover -->
                    <div class="absolute inset-0 rounded-2xl sm:rounded-3xl opacity-0 group-hover:opacity-100 transition-opacity duration-500 pointer-events-none">
                        <div class="absolute inset-0 rounded-2xl sm:rounded-3xl bg-gradient-to-r from-blue-500/10 via-transparent to-violet-500/10"></div>
                    </div>
                    
                    <!-- Video wrapper with loading state -->
                    <div class="relative w-full aspect-video rounded-xl sm:rounded-2xl overflow-hidden bg-slate-100" id="video-container">
                        <!-- Loading placeholder -->
                        <div class="absolute inset-0 flex items-center justify-center bg-gradient-to-br from-slate-100 to-slate-50 z-10 transition-opacity duration-500" id="video-placeholder">
                            <!-- Subtle animated background -->
                            <div class="absolute inset-0 bg-gradient-to-r from-blue-50/50 via-violet-50/50 to-blue-50/50 animate-pulse"></div>
                            
                            <!-- Play button indicator -->
                            <div class="relative flex flex-col items-center gap-4">
                                <div class="w-16 h-16 sm:w-20 sm:h-20 rounded-full bg-white shadow-lg border border-slate-200/60 flex items-center justify-center animate-pulse">
                                    <svg class="w-6 h-6 sm:w-8 sm:h-8 text-slate-400 ml-1" fill="currentColor" viewBox="0 0 24 24">
                                        <path d="M8 5v14l11-7z"/>
                                    </svg>
                                </div>
                                <span class="text-sm text-slate-400 font-medium">Cargando video...</span>
                            </div>
                        </div>
                        
                        <!-- Kinescope Video Embed -->
                        <iframe 
                            id="hero-video"
                            src={`https://kinescope.io/embed/${videoId}?autoplay=0&preload=metadata`}
                            allow="autoplay; fullscreen; picture-in-picture; encrypted-media; gyroscope; accelerometer; clipboard-write;"
                            frameborder="0"
                            allowfullscreen
                            class="absolute inset-0 w-full h-full"
                            title="Demo de AiAutomatiza"
                            onload="document.getElementById('video-placeholder').style.opacity = '0'; setTimeout(() => document.getElementById('video-placeholder').style.display = 'none', 500);"
                        ></iframe>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Scroll indicator - Hidden on mobile -->
    <div 
        class="absolute bottom-8 sm:bottom-12 left-1/2 -translate-x-1/2 hidden md:flex flex-col items-center gap-2 text-slate-400"
        data-animate="fade-up"
        data-animate-delay="600"
    >
        <span class="text-xs font-medium tracking-wide uppercase">Descubre más</span>
        <div class="w-6 h-10 rounded-full border-2 border-slate-300 flex justify-center pt-2">
            <div class="w-1 h-2 bg-slate-400 rounded-full animate-bounce"></div>
        </div>
    </div>
</section>

<script>
    // Video placeholder handling
    const heroVideo = document.getElementById('hero-video');
    const placeholder = document.getElementById('video-placeholder');
    
    if (heroVideo && placeholder) {
        // Hide placeholder when iframe loads
        heroVideo.addEventListener('load', () => {
            placeholder.style.opacity = '0';
            setTimeout(() => {
                placeholder.style.display = 'none';
            }, 500);
        });
        
        // Fallback: hide placeholder after 3 seconds
        setTimeout(() => {
            if (placeholder) {
                placeholder.style.opacity = '0';
                setTimeout(() => {
                    placeholder.style.display = 'none';
                }, 500);
            }
        }, 3000);
    }
</script>
