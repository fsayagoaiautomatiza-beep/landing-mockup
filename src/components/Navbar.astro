---
---

<nav class="fixed top-0 w-full z-50 transition-all duration-300 py-3 sm:py-4 px-4 sm:px-6" id="main-navbar">
    <div class="max-w-7xl mx-auto glass-premium rounded-full px-5 sm:px-8 py-3 sm:py-4 flex justify-between items-center shadow-lg">
        <!-- Logo - Z-pattern: top left -->
        <a href="/" class="flex items-center gap-3 group" id="nav-logo">
            <img 
                src="/logo.webp" 
                alt="AIAutomatiza Logo" 
                class="w-9 h-9 sm:w-10 sm:h-10 group-hover:scale-105 transition-transform duration-300"
                width="40"
                height="40"
            />
            
        </a>

        <!-- Desktop Navigation - Center -->
        <div class="hidden lg:flex items-center gap-8 xl:gap-10">
            <a href="#soluciones" class="nav-link text-sm font-medium">
                Soluciones
            </a>
            <a href="#resultados" class="nav-link text-sm font-medium">
                Resultados
            </a>
            <a href="#clientes" class="nav-link text-sm font-medium">
                Clientes
            </a>
            <a href="#testimonios" class="nav-link text-sm font-medium">
                Testimonios
            </a>
        </div>

        <!-- CTA Button - Z-pattern: top right -->
        <a 
            href="/demo" 
            class="hidden sm:flex items-center gap-2 bg-slate-900 text-white px-5 sm:px-6 py-2.5 sm:py-3 rounded-full font-semibold text-sm hover:bg-slate-800 transition-all duration-200 hover:scale-105 hover:shadow-xl"
            id="nav-cta"
        >
            <span class="hidden md:inline">Agenda una Demo</span>
            <span class="md:hidden">Demo</span>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
            </svg>
        </a>
        
        <!-- Mobile Menu Button -->
        <button 
            class="lg:hidden flex items-center justify-center w-10 h-10 rounded-full hover:bg-slate-100 transition-colors text-slate-700"
            id="mobile-menu-button"
            aria-label="Abrir menú"
            aria-expanded="false"
        >
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
        </button>
    </div>
    
    <!-- Mobile Menu -->
    <div 
        class="hidden lg:hidden mt-3 mx-4 sm:mx-auto glass-premium rounded-2xl p-5 max-w-md shadow-xl"
        id="mobile-menu"
    >
        <div class="flex flex-col gap-1">
            <a href="#soluciones" class="text-center py-3.5 px-4 rounded-xl hover:bg-slate-100 transition-colors text-slate-700 font-medium text-sm">
                Soluciones
            </a>
            <a href="#resultados" class="text-center py-3.5 px-4 rounded-xl hover:bg-slate-100 transition-colors text-slate-700 font-medium text-sm">
                Resultados
            </a>
            <a href="#clientes" class="text-center py-3.5 px-4 rounded-xl hover:bg-slate-100 transition-colors text-slate-700 font-medium text-sm">
                Clientes
            </a>
            <a href="#testimonios" class="text-center py-3.5 px-4 rounded-xl hover:bg-slate-100 transition-colors text-slate-700 font-medium text-sm">
                Testimonios
            </a>
            <div class="mt-3 pt-3 border-t border-slate-200/60">
                <a 
                    href="/demo" 
                    class="flex items-center justify-center gap-2 py-3.5 px-4 rounded-xl bg-slate-900 text-white font-semibold text-sm hover:bg-slate-800 transition-colors"
                >
                    Agenda una Demo
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
                    </svg>
                </a>
            </div>
        </div>
    </div>
</nav>

<script>
    // Mobile menu toggle
    const menuButton = document.getElementById('mobile-menu-button');
    const mobileMenu = document.getElementById('mobile-menu');
    
    if (menuButton && mobileMenu) {
        menuButton.addEventListener('click', () => {
            const isOpen = !mobileMenu.classList.contains('hidden');
            mobileMenu.classList.toggle('hidden');
            menuButton.setAttribute('aria-expanded', String(!isOpen));
        });
        
        // Close menu when clicking a link
        mobileMenu.querySelectorAll('a').forEach(link => {
            link.addEventListener('click', () => {
                mobileMenu.classList.add('hidden');
                menuButton.setAttribute('aria-expanded', 'false');
            });
        });
        
        // Close menu when clicking outside
        document.addEventListener('click', (e) => {
            if (!menuButton.contains(e.target) && !mobileMenu.contains(e.target)) {
                mobileMenu.classList.add('hidden');
                menuButton.setAttribute('aria-expanded', 'false');
            }
        });
    }
    
    // Navbar background on scroll - subtle shadow increase
    const nav = document.getElementById('main-navbar');
    const navInner = nav?.querySelector(':scope > div');
    
    window.addEventListener('scroll', () => {
        if (nav && navInner) {
            if (window.scrollY > 20) {
                navInner.classList.add('shadow-xl');
                navInner.classList.remove('shadow-lg');
            } else {
                navInner.classList.add('shadow-lg');
                navInner.classList.remove('shadow-xl');
            }
        }
    }, { passive: true });
</script>

<style>
    /* xs breakpoint for extra small screens */
    @media (min-width: 400px) {
        .xs\:block {
            display: block;
        }
    }
    
    /* Custom glass effect for navbar */
    .glass-premium {
        background: rgba(255, 255, 255, 0.88);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        border: 1px solid rgba(0, 0, 0, 0.05);
    }
</style>
